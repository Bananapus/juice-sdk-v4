{
  "workspaces": [
    "./packages/juice-sdk-core",
    "./packages/juice-sdk-react"
  ],
  "scripts": {
    "build": "yarn workspaces foreach --all -pt run build",
    "publish": "yarn workspaces foreach --all -pt npm publish"
  },
  "packageManager": "yarn@4.1.1"
}
