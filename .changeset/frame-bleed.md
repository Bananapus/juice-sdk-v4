---
"@bananapus/nana-sdk-connect": patch
---

The framed Center sign-in spans the modal edge to edge with no border of its own: the page inside carries its own inset. The dialog's padding is the `--jb-connect-pad` token (32px; 20px under 540px), which the frame's bleed follows.
