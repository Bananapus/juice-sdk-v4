---
"@bananapus/nana-sdk-core": patch
---

Allow the read-only `eth_simulateV1` method through the JB Center client and EIP-1193 provider so applications can inspect full-transaction simulation logs, including buyback fee fallbacks.
