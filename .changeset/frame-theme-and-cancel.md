---
"@bananapus/nana-sdk-connect": minor
---

The framed Center sign-in takes the app's theme: `JBConnectModal` answers each of the frame's size reports with its resolved `--jb-connect-*` tokens and font (`themeOf(dialog)`), which Center applies inside the frame. The modal's cancel button reads "Cancel" throughout.
