---
"@bananapus/nana-sdk-connect": patch
---

When the tab's saved connection state changed underneath preparing (an earlier attempt settling), the passkey option prepares once more instead of showing "This tab changed its wallet connection".
