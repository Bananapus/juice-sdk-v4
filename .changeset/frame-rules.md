---
"@bananapus/nana-sdk-connect": patch
---

The framed Center sign-in keeps a rule above and below, edge to edge, so the page's own inset reads as a panel. The theme the modal answers size reports with now carries `inset`, the dialog's padding, so the page inside can set its content on the modal's own left edge and keep its mark in the margin.
