---
"@bananapus/nana-sdk-connect": patch
---

Name the platform's prompt on the passkey button: "Continue with Touch ID" on Mac and iPad, "Continue with Face ID" on iPhone, "Continue with Windows Hello" on Windows, and "Continue with a passkey" elsewhere. A `passkeyLabel` prop overrides it, and the `passkeyLabel()` helper is exported.
